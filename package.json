{
  "name": "issue-driven-accessibility-scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/unit/*.test.mjs",
    "lint": "node --check scanner/parse-issue.mjs && node --check scanner/validate-targets.mjs",
    "run:parse": "node scanner/parse-issue.mjs",
    "run:validate": "node scanner/validate-targets.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}